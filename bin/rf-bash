#!/usr/bin/env bash
set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$SCRIPT_DIR"

if docker compose exec -it rf-suite bash 2>/dev/null; then
    exit 0
else
    docker compose run --rm -it rf-suite bash
fi
